<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Phase 7/9: explicit for release builds too — debug/profile
         manifests already carry this for the Flutter tool's own use,
         but release only merges this file, and both RingNowService and
         MediaUploadService need it to reach the Cloudflare Workers. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- mobile_scanner (QR scan on Join Group) — requested at runtime
         too (see QrScanScreen), but Android still needs the manifest
         entry or the runtime request is rejected outright. -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-feature android:name="android.hardware.camera" android:required="false"/>

    <!-- MISSING BEFORE — this whole block. targetSdk is Flutter's
         current default (34/35), so every one of these is enforced by
         the OS, not optional:

         Without SCHEDULE_EXACT_ALARM, AndroidAlarmManager.oneShotAt(...,
         exact: true) is blocked outright on Android 12+ — the local
         alarm never gets scheduled at all, which is the actual reason
         no sounds ever plays: there's nothing to fire.

         Without POST_NOTIFICATIONS, no notification of any kind shows
         on Android 13+ — local or FCM — regardless of channel settings.

         Without USE_FULL_SCREEN_INTENT, Android 14+ can silently
         degrade the alarm to a normal heads-up instead of the
         lock-screen takeover.

         Without RECEIVE_BOOT_COMPLETED, `rescheduleOnReboot: true`
         (alarm_scheduler.dart) has nothing to attach to and does
         nothing after a phone restart.

         Without REQUEST_IGNORE_BATTERY_OPTIMIZATIONS, Doze/App Standby
         can freeze the background isolate before an alarm fires or a
         push is handled — this is also why FCM pushes were silent. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <application
        android:label="kinring"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"
                />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>