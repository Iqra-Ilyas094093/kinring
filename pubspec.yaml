name: kinring
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.11.4

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  # MVVM state management
  provider: ^6.1.2

  # Sora (headings) + Poppins (body) without bundling font files
  google_fonts: ^6.2.1
  firebase_core: ^4.14.0
  firebase_auth: ^6.6.1

  # Phase 2/3 group + event backend already imports this — was missing
  # from pubspec (pre-existing gap, fixed here since Phase 4 depends on
  # GroupEventModel/EventsViewModel compiling).
  cloud_firestore: ^6.0.2

  # Google Sign-In — v7+ uses the new GoogleSignIn.instance API
  # (initialize/authenticate), replacing the old GoogleSignIn().signIn().
  google_sign_in: ^7.2.0

  # Phase 4 — local exact alarm (device-side trigger, works offline).
  # Schedules Android AlarmManager exact alarms; self-persists through
  # reboot via rescheduleOnReboot, no manual BOOT_COMPLETED receiver code
  # needed.
  android_alarm_manager_plus: ^4.0.6

  # Phase 5 — FCM push. Secondary sync layer on top of Phase 4's local
  # alarm (cron backup trigger, admin "Ring Now" broadcast) plus the
  # channel for activity notifications (member cleared/snoozed/joined).
  firebase_messaging: ^16.0.4

  # Full-screen alarm UI when the alarm fires — AlarmManager itself has
  # no UI, so the fire callback shows a fullScreenIntent notification
  # (same mechanism real alarm-clock apps use) that launches the app
  # straight into AlarmRingingScreen/ReminderNotificationCardScreen even
  # from a locked screen.
  flutter_local_notifications: ^19.0.0

  # Contextual runtime permission requests (exact alarm, battery
  # optimization exemption, notifications) — requested on first event
  # create per doc Phase 4 step 5, not as an upfront onboarding flow.
  permission_handler: ^11.3.1

  # Phase 7/9 — plain HTTP client for calling the kinring-ringnow and
  # kinring-media-worker Cloudflare Workers directly (no Firebase SDK
  # wraps these, they're custom HTTP endpoints).
  http: ^1.2.2

  # Phase 9 — picks a profile/group photo from the gallery before
  # MediaUploadService uploads it to the R2 media worker.
  image_picker: ^1.1.2

  # Phase 10 — local-only persistence for Notification/Accessibility
  # Settings toggles (doc explicitly calls these device-only, no cloud
  # sync needed).
  shared_preferences: ^2.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

  # Generates native launcher icons (Android + iOS) from source PNGs below.
  # Run: flutter pub run flutter_launcher_icons
  flutter_launcher_icons: ^0.14.3

# Config for flutter_launcher_icons. image_path is the legacy/fallback icon
# (used as-is on iOS and older Android). adaptive_icon_foreground is a
# separately padded version of the same mark — Android adaptive icons apply
# a circular/rounded/square mask per-device, so the foreground content must
# sit within the center ~66% of the canvas or it gets clipped; the plain
# app_icon.png bleeds almost to the edge, so a padded copy
# (app_icon_foreground.png) was generated for this purpose specifically.
flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/logo/app_icon.png"
  adaptive_icon_background: "#FFFFFF"
  adaptive_icon_foreground: "assets/logo/app_icon_foreground.png"
  min_sdk_android: 21
  remove_alpha_ios: true

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/
    - assets/logo/
    - assets/images/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
